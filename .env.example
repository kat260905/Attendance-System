# Database Configuration
# For Supabase/PostgreSQL (add ?sslmode=require for remote DBs - done automatically in app)
DATABASE_URI=postgresql://user:password@host:5432/database

# For local SQLite (used if DATABASE_URI is not set)
# Leave DATABASE_URI empty to use: sqlite:///attendance.db
